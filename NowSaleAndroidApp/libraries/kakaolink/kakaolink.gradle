evaluationDependsOn(':libraries')

dependencies {
    api project(':libraries:network')
    api project(':libraries:message-template')

    implementation "com.android.support:customtabs:$ANDROID_SUPPORT_LIB_VERSION"
}